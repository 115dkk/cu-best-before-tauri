# CONTEXT — 도메인 용어집

이 프로젝트의 코드·문서·대화에서 쓰는 용어를 여기 고정한다. 코드 식별자는 괄호 안 영문을 그대로 쓴다.

## 도메인

- **조사표 (Sheet)** — 한 번의 소비기한 조사 결과 전체. 작성 시각(`created_at`)으로 식별(`id`)되며, 두 개의 **구역**을 가진다. 로컬 JSON 파일 하나로 저장되고 30일 뒤 정리된다.
- **구역 (Location)** — 상품이 놓인 물리적 공간. 정확히 둘: **매장 (Store)** = 매대에 진열된 것, **워크인 (WalkIn)** = 냉장 창고에 들어 있는 것.
- **품목 (Product)** — 조사 대상 상품 종류. 정확히 다섯이며 순서가 고정된다: 삼각김밥 (Onigiri), 김밥 (Gimbap), 도시락 (Lunchbox), 샌드위치 (Sandwich), 햄버거 (Burger).
- **기한 시각대 (Slot hours)** — 품목마다 소비기한이 만료되는 하루 두 시각. 삼각김밥·김밥·도시락은 **02시·14시**, 샌드위치·햄버거는 **10시·22시**. 사용자는 이 외의 시각을 고를 수 없다.
- **기한 슬롯 (Slot)** — 날짜 + 기한 시각대 하나가 결합된 구체 시각(`NaiveDateTime`, 기기 로컬 시간). 예: 2026-08-30 14:00.
- **항목 (Entry)** — 어느 구역의 어느 품목에 대해 "이 슬롯에 만료되는 상품이 N개 있다"는 기록. `{ at: Slot, quantity: u32 ≥ 1 }`.
- **구역표 (Section)** — 한 구역 안의 품목별 항목 목록. 다섯 품목 키를 항상 모두 가진다(빈 목록 허용).
- **빈 구역 (Empty section)** — 다섯 품목 모두 항목이 0개인 구역표. 저장 이미지에서는 칸 전체를 **X표**로 채운다.
- **후보 슬롯 (Slot options)** — 지금(`now`) 기준으로 사용자가 고를 수 있는 슬롯 목록. **엄격히 미래**(`at > now`)인 슬롯만 포함하며, 가장 가까운 것부터 `horizon_days`일 뒤까지 날짜별로 묶인다. 과거 슬롯 상품은 폐기 대상이라 제외된다.
- **정규화 (Normalize)** — 저장 전에 조사표를 규칙에 맞추는 일: 항목을 시각순 정렬, 같은 슬롯의 항목은 수량을 합쳐 하나로, 잘못된 슬롯(품목의 기한 시각대가 아닌 시각)이나 0 수량은 거부.
- **내보내기 (Export)** — 조사표를 PNG 한 장으로 그려 기기 공용 **사진 폴더(`Pictures/소비기한/`)** 에 저장하는 일. 항목이 없는 품목 행은 **생략**하고, 빈 구역은 X표로 채운다.
- **정리 (Purge)** — 앱 시작 시 `updated_at`이 30일보다 오래된 조사표 파일을 삭제하는 일.

## 아키텍처 용어 (improve-codebase-architecture 스킬 어휘)

- **core 모듈** (`crates/core`) — 도메인 로직 전부. 파일 시스템 경로와 시각을 인자로 받는 순수 함수/타입이며 Tauri에 의존하지 않는다. 테스트 표면이 곧 인터페이스다.
- **shell 모듈** (`src-tauri`) — Tauri 커맨드. `now`와 경로를 구해 core에 넘기고 결과를 JSON으로 돌려주는 얇은 어댑터. 로직을 두지 않는다.
- **프론트엔드** (`src/`) — Svelte 5 화면. 상태의 진실은 항상 shell이 돌려준 조사표 JSON이며, 화면은 그것을 편집해 `save_sheet`로 되돌려 보낸다.
