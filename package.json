{
  "name": "cu-best-before",
  "private": true,
  "version": "0.1.0",
  "description": "CU 소비기한 조사표 작성기 (Tauri 2 / Android)",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json --fail-on-warnings",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6",
    "@tauri-apps/cli": "^2",
    "@types/node": "^24",
    "svelte": "^5",
    "svelte-check": "^4",
    "typescript": "^5.9",
    "vite": "^7"
  }
}
